[pytest]
markers =
    unit: Unit tests (fast, use mocks, no Docker required)
    e2e: End-to-end tests (slow, require Docker)

pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
